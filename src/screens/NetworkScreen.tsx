import React, { FunctionComponent } from "react";
import { Text } from "react-native";

const NetworkScreen: FunctionComponent = ({ }) => {
  return (
    
    <Text>
      Network
    </Text>
  );
};

export default NetworkScreen;
